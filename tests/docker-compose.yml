services:
  mariadb:
    image: mariadb:latest
    restart: always
    env_file:
      - test.env
    ports:
      - "3307:3306"
    healthcheck:
      test: mariadb -u$$MARIADB_USER -p$$MARIADB_PASSWORD -e "SELECT 1;"
      start_period: 3s
      interval: 2s
      retries: 10
